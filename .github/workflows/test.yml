name: Test-workflow
on:
  push:
    branches:
      - main
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: message
        run: echo "hello from test workflow"
      - name: checkout
        uses: actions/checkout@v6.0.2
