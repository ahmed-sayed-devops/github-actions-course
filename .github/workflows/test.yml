on:
  workflow_dispatch:
jobs:
  expressions:
    runs-on: ubuntu-latest
    steps:
      - name: run with PR only
        run: |
          echo " PR event new"
          echo ${{ github.event_name }}
        if: ${{ github.event_name != 'pullrequest' }}
      - name: run with push event
        run: echo "push event new"
        if: ${{ github.event_name == 'Push' }}
        
